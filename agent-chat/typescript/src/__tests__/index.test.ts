/**
 * Test file for the main index module
 */

describe('Jiva.ai Agent Chat SDK', () => {
  it('should export types', () => {
    // Placeholder test - will be expanded as we build the integration
    expect(true).toBe(true);
  });
});

